# Copy this file to .env and fill values
# Preferred: provide the full connection string
MONGODB_URI="mongodb+srv://<username>:<password>@cluster0.bvidyyc.mongodb.net/emailDB?retryWrites=true&w=majority"

# Or provide username/password and DB name (not recommended for production)
DB_USERNAME=<username>
DB_PASSWORD=<password>
DB_NAME=emailDB

# Server port (optional)
PORT=8000
